---
import TableOfContents from './TableOfContents';

const { headings } = Astro.props;
---

<nav>
	{headings && <TableOfContents client:media="(min-width: 50em)" headings={headings} isMobile={false} />}
</nav>
